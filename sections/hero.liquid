{%- liquid
  assign heading = section.settings.heading
  assign subheading = section.settings.subheading
  assign button_label = section.settings.button_label
  assign button_link = section.settings.button_link | default: routes.all_products_collection_url
  assign background_image = section.settings.background_image
  assign overlay_opacity = section.settings.overlay_opacity | divided_by: 100.0
-%}

<section class="hero" {% if background_image != blank %}style="background-image: url('{{ background_image | image_url: width: 1920 }}');"{% endif %}>
  <div class="hero__overlay" style="opacity: {{ overlay_opacity }};"></div>
  <div class="container">
    <div class="hero__content">
      {%- if heading != blank -%}
        <h1 class="hero__heading">{{ heading }}</h1>
      {%- endif -%}
      {%- if subheading != blank -%}
        <p class="hero__subheading">{{ subheading }}</p>
      {%- endif -%}
      {%- if button_label != blank -%}
        <a href="{{ button_link }}" class="hero__button button">
          {{ button_label }}
        </a>
      {%- endif -%}
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--color-gridiron-black);
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    padding: var(--spacing-2xl) var(--spacing-md);
    overflow: hidden;
  }

  @media (min-width: 768px) {
    .hero {
      min-height: 600px;
    }
  }

  @media (min-width: 1024px) {
    .hero {
      min-height: 700px;
    }
  }

  .hero__overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: var(--color-gridiron-black);
    z-index: 1;
  }

  .hero__content {
    position: relative;
    z-index: 2;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .hero__heading {
    font-family: var(--font-heading);
    font-size: clamp(3rem, 8vw, 5.5rem);
    font-weight: 700;
    color: var(--color-white);
    letter-spacing: -0.03em;
    margin-bottom: var(--spacing-6);
    line-height: 1.1;
  }

  .hero__subheading {
    font-family: var(--font-body);
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    font-weight: 400;
    color: var(--color-gray-300);
    margin-bottom: var(--spacing-8);
    line-height: 1.6;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero__button {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-2);
    padding: var(--spacing-4) var(--spacing-8);
    font-size: 1.125rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    background: linear-gradient(135deg, var(--color-teal-600) 0%, var(--color-blue-600) 100%);
    border: none;
    color: var(--color-white);
    border-radius: var(--radius-lg);
    transition: all var(--transition-base);
    box-shadow: 0 10px 25px rgba(13, 148, 136, 0.3), 0 4px 10px rgba(0, 0, 0, 0.2);
  }

  .hero__button:hover {
    background: linear-gradient(135deg, var(--color-teal-700) 0%, var(--color-blue-700) 100%);
    transform: translateY(-2px);
    box-shadow: 0 15px 35px rgba(13, 148, 136, 0.4), 0 6px 15px rgba(0, 0, 0, 0.3);
  }

  .hero__button:active {
    transform: translateY(0);
    box-shadow: 0 8px 20px rgba(13, 148, 136, 0.3), 0 3px 8px rgba(0, 0, 0, 0.2);
  }
</style>

{% schema %}
{
  "name": "Hero",
  "tag": "section",
  "class": "section-hero",
  "settings": [
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background image",
      "info": "Recommended size: 1920x700px"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 90,
      "step": 5,
      "unit": "%",
      "label": "Overlay opacity",
      "default": 40,
      "info": "Controls the darkness of the overlay on the background image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Welcome to SportsLiveDecals"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Custom helmet decals and team branding"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    }
  ],
  "presets": [
    {
      "name": "Hero"
    }
  ]
}
{% endschema %}

